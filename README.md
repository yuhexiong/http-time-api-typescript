# http time API

## Install modules

```bash
npm install
```

## Build

```bash
npm install -g typescript
tsc
```

## Run

```bash
npm install -g nodemon
nodemon build/server.js
```

## API

* `GET /time`
* `POST /time`
* `PUT /timezone`
* `DELETE /timezone`